package MultiThreading;

public class WhatisSynchronizedKeyword {

	// Synchronization
	/******
	 * Synchronization means suppose It is the Process Of enabling lock of an Object
	 * ,By Default Every Java Object is assoicated with Locking Mechanism default it
	 * is disabled ....
	 * 
	 * If u want to enable the lock so it will work in multithreaded Environment
	 * there u can use Synchronizatin just Allow the lock of the Object so that
	 * Multiple threds should not Allowed that object concurrently
	 * 
	 * 
	 * 
	 * 
	 * 
	 */

}
